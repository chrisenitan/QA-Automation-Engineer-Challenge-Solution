import { en } from './en';
import { de } from './de';

/**
 * Application language strings
 * Avoid using language strings as test selectors, allowed in very few cases.
 */
export const language = {
  en,
  de
};
